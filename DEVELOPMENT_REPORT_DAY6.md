# 공공기관 업무추진비 맛집 지도 서비스 개발 진행 보고서 6일차

작성일: 2026-07-03

## 1. 프론트엔드

6일차 프론트엔드 개발은 운영자 화면을 실제 업무 단계에 맞게 분리하고, 사용자 지도 화면의 검색·필터 경험을 개선하는 데 집중했다. Day5까지는 수집·검증 작업판이 하나의 흐름으로 묶여 있었지만, 이번 변경에서는 운영자가 지금 해야 할 일이 수집인지, 파싱인지, 검토인지 명확히 구분되도록 화면 구조를 다시 정리했다.

Day5 보고서 이후 프론트엔드 관련 변경은 크게 네 가지다.

```text
기준 지점:
e9cf5a3 Add workflow progress tracking and controls

이후 커밋됨:
bd377a8 Refresh project README
43aef73 Prepare repository for public release

현재 작업트리:
관리자 작업판 분리, 문서 파싱 상태 표시, 지도 검색/필터 개선, 테스트 보강
```

### 관리자 화면을 업무 단계별로 분리

관리자 대시보드의 내비게이션과 작업 화면을 다음처럼 분리했다.

```text
대시보드: /admin
수집 작업판: /admin/collection
파싱 작업판: /admin/parsing
검토 작업판: /admin/review
문서 게시판: /admin/documents
운영 로그: /admin/logs
```

기존 `/admin/workflow`는 파싱 작업판으로 연결되도록 유지해 기존 접근 경로가 깨지지 않게 했다. 운영자 입장에서는 한 화면에 수집, 파싱, 검증 기능이 모두 섞여 있는 구조보다 각 단계의 목적과 버튼이 더 명확해졌다.

수집 작업판은 기관 목록 조회와 문서 수집에 집중한다. 파싱 작업판은 수집된 문서에서 집행 내역을 추출하는 기능에 집중한다. 검토 작업판은 검증 대기 후보를 승인, 수동검토, 반려 상태로 분류하는 기능에 집중한다.

### 작업판 단계 UI 재구성

작업판 상단 단계 표시를 실제 데이터 처리 흐름에 맞게 확장했다.

```text
1. 기간 설정
2. 목록 가져오기
3. 수집 배치
4. 문서 수집
5. 문서 파싱
6. 검증 배치
7. 결과 분류
```

Day5에서는 수집과 검증 사이의 중간 단계가 상대적으로 숨겨져 있었다. 6일차 변경에서는 수집된 원문 문서와 실제 집행 행 추출 사이에 `파싱` 단계를 별도로 드러냈다. 이로 인해 운영자는 “문서는 수집됐지만 아직 파싱되지 않은 상태”, “파싱은 실패했지만 재시도 가능한 상태”, “파싱은 끝났고 검증만 남은 상태”를 UI에서 구분할 수 있다.

작업판 프론트엔드에는 다음 기능이 추가되었다.

- 화면 모드별 렌더링: 수집, 파싱, 검토
- 파싱 실행 버튼
- 파싱 실패 문서 재시도 버튼
- 문서별 파싱 상태 배지
- 파싱 상태 필터
- 작업 로그의 세션 저장
- 수집·파싱·검증 단계별 진행 메시지 표시

### 문서 게시판과 상세 화면의 파싱 상태 표시

문서 게시판과 문서 상세 화면에도 파싱 상태를 표시하도록 개선했다.

문서 목록에서는 기존 수집 상태만 보는 것이 아니라 다음 정보를 함께 볼 수 있다.

- 수집 상태
- 파싱 상태
- 파싱 시도 횟수
- 파싱 실패 사유
- 집행 행 추출 여부
- 검증 진행률

문서 상세 화면에서도 개별 문서가 파싱 완료인지, 실패인지, 지원하지 않는 형식인지 확인할 수 있다. 특히 DRM 문서나 구조가 맞지 않는 파일처럼 자동 파싱이 어려운 경우, 실패 사유를 운영자가 바로 확인할 수 있게 했다.

### 지도 화면 검색과 필터 개선

사용자용 지도 화면에서는 지역 검색과 방문 횟수 필터를 개선했다.

검색어가 특정 지역으로 해석될 수 있는 경우 지도 중심을 해당 지역으로 이동시키고, 주소 기반 검색 모드로 식당 목록을 조회한다. 예를 들어 `부산`, `서울`, `대전`, `천안`, `대구`, `인천`, `광주`, `울산`, `세종` 같은 지역 키워드에 대해 지도 초점과 검색 조건이 함께 조정된다.

지도 화면에는 방문 횟수 필터도 추가했다.

```text
전체
10회 이상
```

이 필터는 단순히 화면에서만 숨기는 방식이 아니라 API 요청 파라미터로 전달된다. 따라서 백엔드에서 실제 집계 기준에 맞는 식당만 내려주고, 프론트엔드는 그 결과를 지도 마커와 랭킹 목록에 반영한다.

지도 마커와 랭킹 목록도 함께 개선했다.

- 선택된 식당 마커 강조
- 랭킹 목록의 선택 상태 표시
- 검색 결과에 맞춘 지도 중심 이동
- 검색 결과가 없을 때의 빈 상태 표시
- 네이버 지도 링크 유지
- 금액 중심 표시보다 방문 횟수와 장소 정보 중심으로 정리

### 공개 릴리스와 문서 산출물 정리

6일차에는 코드 화면 외에도 외부 공개를 고려한 산출물을 정리했다.

커밋된 변경에는 다음 항목이 포함된다.

- `README.md` 최신 구현 기준 재작성
- `.gitignore` 정리
- 플로우차트 및 UI 설계 PPT 추가
- 공개 저장소 전환을 위한 민감 정보 점검과 이력 정리

README는 현재 구현된 관리자 기능, 데이터 수집 흐름, 검증 파이프라인, 실행 방법을 기준으로 다시 정리했다. 블로그나 포트폴리오에서 프로젝트의 현재 상태를 설명할 때 사용할 수 있는 기준 문서 역할을 한다.

## 2. 백엔드

6일차 백엔드 개발의 핵심은 수집과 파싱을 분리한 것이다. Day5까지는 문서를 수집하는 과정에서 곧바로 집행 행 파싱까지 이어지는 구조였다. 이 구조에서는 원문 문서 수집은 성공했지만 파싱만 실패한 경우를 독립적으로 다루기 어려웠다. 이번 변경에서는 수집 상태와 파싱 상태를 별도로 저장하고, 파싱 실패 문서만 재시도할 수 있도록 파이프라인을 확장했다.

### 수집 상태와 파싱 상태 분리

데이터베이스 스키마에 파싱 상태 필드를 추가했다.

```text
parse_status
parse_attempts
parse_error_message
parsed_at
```

이 필드는 수집 계획 문서와 원문 문서에 적용된다. 기존 데이터베이스에도 안전하게 반영되도록 마이그레이션 로직을 추가했다. 이미 집행 행이 존재하는 기존 문서는 파싱 완료 상태로 보정하고, 수집은 되었지만 집행 행이 없는 문서는 현재 상태에 맞게 파싱 대기 또는 실패 상태로 남긴다.

파싱 상태는 다음처럼 관리된다.

```text
not_requested: 아직 파싱 요청 전
parsing: 파싱 진행 중
parsed: 파싱 성공
empty: 파싱은 성공했지만 추출 행 없음
failed: 파싱 실패
unsupported: 자동 파싱을 지원하지 않는 문서
```

이 분리로 운영자는 “수집 실패”와 “파싱 실패”를 다른 문제로 다룰 수 있다. 수집 실패는 원문 다운로드 문제이고, 파싱 실패는 파일 형식이나 엑셀 구조 문제이기 때문이다.

### 파싱 전용 파이프라인 추가

수집 계획 실행 흐름에 파싱 전용 메서드를 추가했다.

```text
parse_collection_plan_batch
parse_collection_plan_batches
retry_collection_plan_parse_failures
```

수집 배치는 원문 문서를 저장하는 데 집중하고, 파싱 배치는 저장된 원문 문서를 읽어 집행 행을 추출한다. 실패한 파싱은 별도 재시도 API를 통해 다시 실행할 수 있다.

자동 재시도 정책도 구분했다.

- 일반 실패 문서는 재시도 대상
- 지원하지 않는 형식의 문서는 자동 재시도 제외
- 이미 파싱 완료된 문서는 중복 파싱 제외
- 집행 행이 이미 존재하는 문서는 기존 파싱 완료 상태로 보정

이 구조는 향후 PDF, HTML, XLSX 등 파일 형식별 파서가 늘어나도 안정적으로 확장할 수 있다.

### 파싱 API와 관리자 라우트 추가

운영 API에 파싱 실행 엔드포인트를 추가했다.

```text
POST /ops/collection-plans/{plan_id}/parse
POST /ops/collection-plans/{plan_id}/retry-parse-failed
```

문서 목록 API도 파싱 상태 필터를 받을 수 있게 확장했다.

```text
GET /admin/documents/data?parse_status=...
GET /admin/documents/data?plan_id=...
```

관리자 HTML 라우트도 역할별로 정리했다.

```text
GET /admin
GET /admin/collection
GET /admin/parsing
GET /admin/workflow
GET /admin/review
GET /admin/review/results
```

`/admin/review`는 검토 작업판으로 사용하고, 기존 승인·수동검토·반려 항목 편집 화면은 `/admin/review/results`로 분리했다. 이로써 “검증 실행 작업”과 “검증 결과 수정 작업”이 구분된다.

### 엑셀 파서 보강

업무추진비 원문 엑셀은 기관마다 표 구조와 헤더 이름이 다르다. 6일차에는 실제 부산 공공데이터 문서의 변형을 더 잘 처리하도록 엑셀 파서를 보강했다.

주요 개선 사항은 다음과 같다.

- 여러 시트를 모두 읽도록 변경
- `거래처`, `거래처명` 같은 상호명 헤더 추가 인식
- `집행금액`, `적요`, `집행방식`, `명령구분` 등 기관별 헤더 별칭 추가
- `YYYYMMDD`, `YYMMDD` 날짜 형식 처리
- 엑셀 serial date 처리
- `2026. 1. 30.`, `26.1.27.(화)` 같은 날짜 처리
- `25년 12월 중`처럼 불완전한 날짜 표현 처리
- 제목이나 상단 행의 연도를 참고해 `10.17.` 같은 월일 표현 보정
- HTML 형식으로 저장된 엑셀 파일 처리 유지

이 변경으로 기관마다 조금씩 다른 엑셀 양식을 수동 보정 없이 더 많이 흡수할 수 있게 됐다.

### 지도 API 집계와 검색 조건 개선

지도 식당 목록 API에 방문 횟수 필터와 검색 모드를 추가했다.

```text
GET /api/map/restaurants?min_visit_count=10
GET /api/map/restaurants?q=부산&search_mode=address
```

`min_visit_count`는 방문 횟수 기준으로 식당을 필터링한다. 현재는 10회 이상 같은 운영자가 의도한 기준만 허용하도록 검증한다. 잘못된 값은 서버에서 오류로 처리해, 화면과 API 기준이 어긋나지 않게 했다.

검색 모드는 일반 검색과 주소 검색을 구분한다. 지역명 검색은 상호명보다 주소에 매칭되어야 하므로 `search_mode=address`를 사용한다. 이를 통해 `서울`처럼 상호명에도 들어갈 수 있는 단어가 지역 검색 의도와 섞이는 문제를 줄였다.

식당별 집계 쿼리도 개선했다. 기존에는 지출 행과 리뷰 집계를 함께 조인하면서 집계가 중복될 가능성이 있었다. 이번 변경에서는 지출 통계와 검증 통계를 각각 CTE로 먼저 계산한 뒤 식당 목록에 결합하도록 정리했다.

### 후보 상태 집계 보정

관리자 후보 목록에서 `pending`과 `needs_review`의 의미를 분리했다.

```text
pending: 아직 검증되지 않은 대기 항목
needs_review: 검증 결과 수동검토가 필요한 항목
```

기존에는 화면에서 두 상태가 섞여 보일 여지가 있었다. 이번 변경에서는 검증 대기 수와 실제 수동검토 수를 별도로 집계한다. 운영자는 아직 AI 검증을 돌리지 않은 항목과, AI 검증 결과 사람이 확인해야 하는 항목을 구분해서 볼 수 있다.

### 테스트와 현재 상태

6일차 변경은 테스트를 함께 보강했다.

수정된 테스트 범위는 다음과 같다.

- HTTP 라우트 및 관리자 화면 테스트
- 수집 계획과 파싱 배치 테스트
- 파싱 실패 및 재시도 정책 테스트
- 지도 검색과 방문 횟수 필터 테스트
- 후보 상태 집계 테스트
- 부산 원문 엑셀 파서 테스트

현재 전체 테스트 결과는 다음과 같다.

```text
python3 -m unittest discover -s tests

Ran 106 tests in 2.759s
OK
```

현재 작업트리 기준 미커밋 변경은 19개 파일이며, 규모는 다음과 같다.

```text
19 files changed, 2717 insertions(+), 462 deletions(-)
```

이번 6일차 개발을 통해 서비스는 단순히 데이터를 가져와 검증하는 수준에서, 운영자가 수집·파싱·검토 단계를 분리해 관리할 수 있는 구조로 발전했다. 특히 파싱 상태가 독립적인 운영 단위가 되면서 실제 공공기관 문서의 다양한 파일 형식과 실패 케이스를 추적하고 재처리할 수 있는 기반이 마련됐다.
