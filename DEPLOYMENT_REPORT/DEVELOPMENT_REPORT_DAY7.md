# 공공기관 업무추진비 맛집 지도 서비스 개발 진행 보고서 7일차

작성일: 2026-07-05

## 1. 프론트엔드

7일차 개발은 사용자에게 보이는 공개 지도 화면의 완성도를 높이는 데 집중했다. 6일차까지는 지도 검색, 방문 횟수 필터, 지역 검색의 기본 흐름이 들어갔다. 이번 변경에서는 서비스명을 `공기밥`으로 정리하고, 부산 안에서 실제 사용자가 검색할 가능성이 높은 구·상권·동네 단위 검색을 더 촘촘하게 지원했다.

이번 보고서의 기준은 6일차 보고서가 포함된 다음 커밋이다.

```text
기준 지점:
3f550ad Separate collection parsing and improve map filters

현재 변경 상태:
미커밋 작업트리 6개 파일 수정
6 files changed, 299 insertions(+), 27 deletions(-)
```

### 서비스명 `공기밥` 반영

공개 지도 화면의 브랜드명을 기존 `공공 맛집 지도`에서 `공기밥`으로 변경했다.

변경된 위치는 다음과 같다.

- 브라우저 탭 제목
- 공개 지도 상단 브랜드 텍스트
- 기본 앱 설정값
- HTTP 서버 테스트 기대값

이제 서비스의 첫 화면에서 프로젝트명이 기능 설명형 이름이 아니라 실제 서비스명으로 노출된다. 블로그나 포트폴리오에서 보여줄 때도 화면 상단에 `공기밥`이라는 이름이 일관되게 표시된다.

### 부산 구 단위 지역 검색 확장

부산 지역 검색을 시·도 단위에서 구·군 단위로 확장했다. 사용자가 `해운대구`, `수영구`, `기장군`처럼 부산 안의 행정구역을 입력하면 해당 지역 중심으로 지도가 이동하고, 주소 검색 조건도 해당 지역으로 보정된다.

추가된 주요 검색 대상은 다음과 같다.

- 중구
- 서구
- 동구
- 영도구
- 부산진구
- 남구
- 북구
- 사하구
- 금정구
- 강서구
- 연제구
- 수영구
- 사상구
- 기장군
- 동래구

각 지역에는 중심 좌표, 지도 줌 레벨, 검색 경계값을 함께 지정했다. 따라서 단순히 검색어만 바꾸는 것이 아니라, 지도 화면의 위치와 API 조회 범위가 같이 좁혀진다.

### 부산 주요 상권·동네 검색 추가

행정구역 외에도 실제 사용자가 맛집을 찾을 때 자주 입력할 만한 부산 상권·동네 키워드를 추가했다.

추가된 주요 키워드는 다음과 같다.

- 서면
- 전포
- 전포카페거리
- 광안리
- 센텀
- 센텀시티
- 부산대
- 부대앞
- 경성대
- 부경대
- 남포
- 남포동
- 자갈치
- 동래
- 연산
- 정관
- 일광

예를 들어 `광안리`를 검색하면 수영구 전체보다 더 좁은 광안리 주변 경계로 조회한다. `서면`이나 `전포`도 부산진구 전체가 아니라 실제 상권 중심에 맞춰 지도가 이동한다.

이 변경으로 사용자는 행정구역명을 정확히 몰라도 익숙한 지역명으로 결과를 탐색할 수 있다.

### 경계값 기반 지도 검색

지역 검색 항목에 `useBounds`와 `viewport` 값을 추가했다. 해당 값이 있는 지역은 API 요청에 `bounds` 파라미터를 함께 전달한다.

```text
q=부산광역시 수영구
search_mode=address
bounds=35.145,129.108,35.162,129.13
```

기존 지역 검색은 주소 문자열 매칭에 의존했다. 이번 변경에서는 지도 화면에서 보고 있는 영역까지 검색 조건에 포함하므로, 이름만 지역과 비슷한 식당이 결과에 섞일 가능성이 줄었다.

예를 들어 `기장` 검색에서는 다음 두 경우를 구분할 수 있다.

- 상호명에 `기장`이 들어간 부산 연제구 식당
- 실제 주소가 부산 기장군인 식당

일반 검색에서는 둘 다 검색될 수 있지만, 지역 검색 모드에서는 주소와 경계값을 기준으로 실제 기장군 결과만 남긴다.

### 주소성 검색 판단 개선

기존에는 `시`, `군`, `구`, `동`처럼 행정구역 접미사가 붙은 검색어를 지역 검색으로 판단했다. 7일차 변경에서는 주소로 볼 수 있는 패턴을 더 넓게 인식하도록 개선했다.

새로 처리하는 주소성 검색 패턴은 다음과 같다.

- 시·군·구·읍·면·동·리 단위 주소
- `광역시`, `특별시`, `특별자치시`, `특별자치도` 포함 주소
- 도로명 주소: `대로`, `로`, `길` + 숫자
- 지번 주소: 숫자 또는 산 지번
- 우편번호 형식

이를 통해 사용자가 상호명 대신 주소 일부를 입력했을 때도 상호 검색이 아니라 주소 검색으로 처리된다.

### 지도 이동 동작 보정

지도 초점 이동 로직을 공통 함수로 정리했다.

```text
focusNaverMapOnPoint
focusNaverMapOnRestaurant
focusNaverMapOnRegion
```

지역 검색에서는 지정한 줌 레벨로 정확히 이동하고, 개별 식당 선택에서는 사용자가 이미 더 가까이 확대해 둔 경우 그 확대 상태를 유지한다. 검색 결과 탐색과 개별 식당 클릭의 지도 동작이 분리되어 사용성이 좋아졌다.

## 2. 백엔드

7일차 백엔드 변경은 공개 지도 화면의 입력을 서버가 안정적으로 받아들이도록 연결하고, 테스트로 검색 의미를 고정하는 데 집중했다. 큰 구조 변경보다는 프론트엔드 검색 경험을 뒷받침하는 설정, 렌더링, 검증 보강이 중심이다.

### 앱 이름 설정값 연결

설정 객체의 기본 앱 이름을 `공기밥`으로 변경했다.

```text
app_name: 공기밥
```

HTTP 서버는 공개 인덱스 HTML을 만들 때 이 설정값을 전달한다. 뷰 함수는 전달받은 앱 이름을 HTML escape 처리한 뒤 `<title>`과 상단 브랜드 영역에 렌더링한다.

이 구조로 바꾼 이유는 서비스명을 화면에 하드코딩하지 않고 설정값으로 관리하기 위해서다. 이후 배포 환경이나 소개용 빌드에서 이름을 바꾸더라도 서버 설정만 바꾸면 공개 화면에 반영할 수 있다.

### 공개 인덱스 렌더링 인터페이스 변경

공개 화면 렌더링 함수의 시그니처를 다음처럼 변경했다.

```text
public_index(naver_map_key, app_name="공기밥")
```

기존에는 네이버 지도 키만 받아 화면을 만들었다. 이제는 앱 이름도 함께 받아서 HTML 문서 제목과 브랜드 표시를 구성한다.

변경된 호출 흐름은 다음과 같다.

```text
HTTP 요청 /
-> app.settings.app_name 조회
-> public_index(naver_map_key, app_name)
-> 공개 지도 HTML 응답
```

### 지역 검색 테스트 보강

지도 검색의 의미가 바뀌지 않도록 서비스 테스트를 추가했다.

추가된 핵심 검증은 다음과 같다.

- `bounds`가 있는 수영구 검색은 광안리 주변 결과만 반환한다.
- 일반 검색에서 `기장`은 상호명과 주소를 모두 검색한다.
- 지역 검색에서 `부산광역시 기장군`은 실제 주소가 기장군인 식당만 반환한다.
- 상호명에만 `기장`이 들어간 다른 지역 식당은 지역 검색 결과에서 제외된다.

이 테스트는 프론트엔드에서 `bounds`를 보내는 변경과 백엔드 지도 API의 주소 기반 검색 의미가 맞물려 동작하는지 확인한다.

### HTTP 화면 테스트 갱신

공개 인덱스 화면의 기대 문구를 `공기밥`으로 갱신했다. 이 테스트는 서버가 루트 경로(`/`)에서 공개 지도 HTML을 정상 반환하고, 브랜드명과 지도 필터 UI가 함께 렌더링되는지 확인한다.

현재 검증 결과는 다음과 같다.

```text
python3 -m unittest discover -s tests

Ran 107 tests in 3.129s
OK
```

7일차 변경으로 `공기밥`은 기능 중심의 내부 프로젝트 화면에서, 사용자가 실제로 지역명을 입력해 탐색할 수 있는 공개 서비스 화면에 더 가까워졌다. 특히 부산의 구·상권 단위 검색과 경계값 기반 필터링이 들어가면서, 지도 검색 결과가 사용자의 지역 검색 의도에 더 정확히 맞춰지게 됐다.
